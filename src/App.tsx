import Layout from "./components/layout/layout";

function App() {
	return (
		// Keep our App component nice and neat so we can add app-level features here later
		// 		e.g. UserLogin, or Theme components
		<Layout />
	);
}

export default App;
